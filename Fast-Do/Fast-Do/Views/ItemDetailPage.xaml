<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Fast_Do.Views.ItemDetailPage"
             Title="{Binding Title}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Remover" Clicked="Remove_Clicked" />
    </ContentPage.ToolbarItems>

    <StackLayout Spacing="20" Padding="15">
        <Label Text="{Binding Item.Text}" d:Text="Título" x:Name="lblTitle" FontSize="Small"/>
        <Label Text="{Binding Item.Description}" d:Text="Descrição" x:Name="lblDesc" FontSize="Small"/>
    </StackLayout>
    
</ContentPage>